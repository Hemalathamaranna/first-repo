import jenkins.automation.builders.PipelineJobBuilder

def script = """
    pipeline {
        agent any
        stages {
            stage('hello') {
                steps {
                    sh 'echo "Hello World"'
                }
            }
        }
    }
